import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Calcu-Proyecto-UX',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
